# Redis Clone

A Redis-inspired in-memory key-value store built in Python using TCP sockets.

## Goals

- Learn socket programming
- Build a command parser
- Support Redis-like commands
- Add TTL expiration
- Add persistence
- Handle multiple clients
